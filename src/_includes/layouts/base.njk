<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{% if title %}{{ title }} · {% endif %}{{ site.title or "The Argosy" }}</title>

  {# Google Fonts: Newsreader (classic news vibe) #}
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <link href="https://fonts.googleapis.com/css2?family=Newsreader:opsz,wght@6..72,400;6..72,500;6..72,600;6..72,700&display=swap" rel="stylesheet" />

  <link rel="stylesheet" href="/assets/style.css" />
  <style>
    :root{
      --font-body: "Newsreader", ui-serif, Georgia, "Times New Roman", Times, serif;
      --font-ui: ui-sans-serif, system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial, "Noto Sans", "Apple SD Gothic Neo", sans-serif;
      --ink: #111827;        /* near-black */
      --muted: #6b7280;      /* gray-500 */
      --rule: #e5e7eb;       /* gray-200 */
      --link: #1d4ed8;       /* blue-700 */
      --bg:   #ffffff;
      --accent: #111827;
      --container: 1160px;   /* max width */
    }
    html,body{margin:0;padding:0;background:var(--bg);color:var(--ink);}
    body{font-family:var(--font-body); font-size:18px; line-height:1.6;}
    a{color:var(--ink); text-decoration:none;}
    a:hover{text-decoration:underline;}
    /* Header */
    .site-header{border-bottom:1px solid var(--rule); padding:18px 16px;}
    .site-header__inner{max-width:var(--container); margin:0 auto; display:flex; align-items:center; justify-content:space-between; gap:12px;}
    .brand{font-weight:700; letter-spacing:0.02em; font-size:28px;}
    .nav{font-family:var(--font-ui); font-size:15px; color:var(--muted); display:flex; gap:14px; flex-wrap:wrap;}
    .nav a{color:inherit;}
    /* Footer */
    .site-footer{border-top:1px solid var(--rule); padding:28px 16px; margin-top:40px; color:var(--muted); font-family:var(--font-ui); font-size:14px;}
    .site-footer__inner{max-width:var(--container); margin:0 auto; display:flex; justify-content:space-between; flex-wrap:wrap; gap:12px;}
    /* Container utility */
    .wrap{max-width:var(--container); margin:0 auto; padding:0 16px;}
  </style>
</head>
<body>
  <header class="site-header">
    <div class="site-header__inner">
      <a href="/" class="brand">{{ site.title or "The Argosy" }}</a>
      <nav class="nav">
        <a href="/">Home</a>
        <a href="/category/politics/">Politics</a>
        <a href="/category/sports/">Sports</a>
        <a href="/category/culture/">Culture</a>
        <a href="/category/health/">Health</a>
      </nav>
    </div>
  </header>

  <main class="wrap">
    {{ content | safe }}
  </main>

  <footer class="site-footer">
    <div class="site-footer__inner wrap">
      <div>© {{ "now" | date("yyyy") }} {{ site.title or "The Argosy" }}</div>
      <div>{{ site.description or "Independent student journalism." }}</div>
    </div>
  </footer>
</body>
</html>
