<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ title or site.title }}</title>
    <link rel="stylesheet" href="{{ '/assets/css/base.css' | url }}">
    {% if site.favicon %}<link rel="icon" href="{{ site.favicon | url }}">{% endif %}
  </head>

  <body>
    <!-- Slim top bar: only the pinned pages -->
    <header class="topbar">
      <div class="container">
        <nav class="topnav" aria-label="Utility navigation">
          <a href="/">Home</a>
          <a href="/about/">About Us</a>
          <a href="/team/">Meet the Team</a>
          <a href="/join/">Join Us</a>
        </nav>
      </div>
    </header>

    <!-- Your theme’s existing masthead/hero/categories will render inside {{ content }} -->
    <main class="site-content">
      {{ content | safe }}
    </main>

    <footer class="site-footer">
      <div class="container">
        <p>&copy; {{ now | date('yyyy') }} {{ site.title }}. All rights reserved.</p>
      </div>
    </footer>
  </body>
</html>
